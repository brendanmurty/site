{% set posts = search.pages("type=post", "date=desc") %}
<ul class="brendan_posts">
{% for post in posts %}
{% if post.url != '/brendan/posts/' %}
  <li>
    <span class="post-date">
      {{ post.date | date('d LLL yyyy') }}
    </span>
    <a class="post-link" href="{{ post.url }}">
      {% if post.title %}
      {{ post.title }}
      {% else %}
      {{ post.url }}
      {% endif %}
    </a>
  </li>
{% endif %}
{% endfor %}
</ul>